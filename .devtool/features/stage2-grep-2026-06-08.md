---
id: "stage2-grep-2026-06-08"
status: "backlog"
priority: "medium"
assignee: null
dueDate: null
created: "2026-06-08T18:03:21.000Z"
modified: "2026-06-08T18:03:21.000Z"
completedAt: null
labels: ["stage-2", "rust-core"]
order: "aG"
---

# Stage 2 — grep

## Acceptance
- [ ] Поля: `pattern`, `collection?`, meta-фильтры, `ignore_case`, `multiline?`, `context_lines?`, `max_matches`, `max_per_doc?`
- [ ] `regex` крейт (RE2): линейное время, без backreferences/lookaround → нет ReDoS; `RegexBuilder` для case-insensitive
- [ ] Возврат: `{doc_id, name, line_number, line_text, context_before[]?, context_after[]?}` + `truncated`, `total_found`
- [ ] Битый паттерн → **структурная ошибка тула** (не падение транспорта/сессии)
- [ ] Обслуживается **сразу после accept** (по `text`), не ждёт векторов
- [ ] **ПРЕДУСЛОВИЕ (carried-forward из Stage 1):** требует синхронной установки `text` на accept (см. [[stage2-get-segment]]) — сейчас `store::accept_index` откладывает весь документ в воркер, текст недоступен до эмбеддинга

Refs: §5.3, §3 (regex), §4.4.
